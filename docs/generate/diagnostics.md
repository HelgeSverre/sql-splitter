# Generate diagnostics

The canonical catalog is
[sql-splitter.dev/commands/generate/diagnostics](https://sql-splitter.dev/commands/generate/diagnostics/).

Every built-in code has an exact case-preserving anchor. For example:

<https://sql-splitter.dev/commands/generate/diagnostics/#GEN-INFER-PLANNER-NOMINATE>

The definitions that drive code usage and summaries live in the canonical Rust
diagnostic catalog; see [maintainers.md](maintainers.md).
